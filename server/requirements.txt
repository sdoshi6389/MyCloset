# ── Core web framework ──────────────────────────────────────────────
Flask==3.1.1
flask-cors==6.0.1
gunicorn==23.0.0

# ── Database ────────────────────────────────────────────────────────
psycopg2-binary==2.9.10
supabase==2.31.0

# ── Auth & security ─────────────────────────────────────────────────
Werkzeug==3.1.3
PyJWT==2.13.0
python-dotenv==1.2.2

# ── ML / AI ─────────────────────────────────────────────────────────
# CPU wheels — Railway has no GPU. This pulls slim CPU torch (~200MB) instead
# of the ~2.5GB CUDA build. Keep this --extra-index-url line ABOVE the pins.
--extra-index-url https://download.pytorch.org/whl/cpu
torch==2.7.1+cpu
torchvision==0.22.1+cpu
transformers==4.53.2
faiss-cpu==1.11.0.post1
Pillow==11.3.0
pillow-heif==1.0.0
numpy==1.26.4
opencv-python==4.11.0.86

# ── OCR / detection ─────────────────────────────────────────────────
ultralytics==8.3.170
easyocr==1.7.2
realesrgan==0.3.0
basicsr==1.4.2
gfpgan==1.3.8

# ── Image processing ────────────────────────────────────────────────
rembg==2.0.67
onnxruntime==1.27.0
mediapipe==0.10.21

# ── External APIs ───────────────────────────────────────────────────
openai==1.99.3
replicate==1.0.7

# ── Scrapers (run separately; pinned for parity with the local env) ─
playwright==1.53.0
requests==2.32.4
beautifulsoup4==4.13.4
pandas==2.3.1
tqdm==4.67.1
