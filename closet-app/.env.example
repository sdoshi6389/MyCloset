# Base URL of the deployed backend (Flask) API — no trailing slash.
# Copy this file to .env.local for local overrides, and set the same key
# in Vercel → Project Settings → Environment Variables for production.
#
# Local dev leaves this unset and the app falls back to http://localhost:5000.
VITE_API_BASE=https://your-backend-host.example.com
